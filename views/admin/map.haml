%ul
  - Menu.all.each do |menu|
    %li
      %a{:href => "/admin/menu/#{menu.id}"}
        = menu.name
    %ul
      - menu.entry_menus.sort_by { |em| em.ordr }.each do |em|
        %li
          %a{:href => "/admin/entry/#{em.entry.id}"}
            = em.title
            = "(#{em.entry.title})"


  
